<?php
function updateData($mail,$usrname,$frstName,$lstName,$token){

echo$mail,$usrname,$frstName,$lstName,$token;
// require_once '../Engine/connect.php';
// global $conn;

// // Correction de la requête SQL
// $stmt = $conn->prepare("UPDATE * FROM users WHERE verification_token = :tkn");
// $stmt->execute([':tkn' => $tkn]);

// $user = $stmt->fetch(PDO::FETCH_ASSOC);

// if ($user && password_verify($pswrd, $user['password_hash'])) {
//     return true;
// }
// return false;
}